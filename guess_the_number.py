# Let's write a program where the user has x trials to guess the a random number between min_n and max_n.
# If the user has correctly guessed the user wins, otherwise the user looses.

X = 10
MIN_N = 1
MAX_N = 100
